# Story 7.1: Identity Map Pattern Implementation

## Status

Approved

## Story

**As a** developer,  
**I want** the Identity Map pattern implemented,  
**So that** I can ensure each object loads only once.

## Acceptance Criteria

1. Identity Map preventing duplicate object loading
2. Cache management for loaded objects
3. Unit tests for identity mapping
4. Documentation on ObjectScript caching
5. Healthcare example for patient object caching

## Tasks / Subtasks

- [ ] Task 1: Create Identity Map Core Pattern (AC: 1, 2)
  - [ ] Create src/Patterns/PoEAA/ObjectRelational/IdentityMap.cls base class
  - [ ] Implement Add(pId, pObject) to store object in map
  - [ ] Implement Get(pId) to retrieve object from map
  - [ ] Implement Contains(pId) to check if object exists in map
  - [ ] Implement Remove(pId) to remove object from map
  - [ ] Implement Clear() to empty entire map
  - [ ] Create internal storage using multidimensional property or %ArrayOfObjects
  - [ ] Implement GetOrLoad(pId, pLoader) pattern for lazy loading
  - [ ] Add statistics tracking (hits, misses, size)
  - [ ] Implement thread-safe access if needed
  - [ ] Document identity map usage patterns

- [ ] Task 2: Create Healthcare Patient Identity Map Example (AC: 5)
  - [ ] Create src/Patterns/Examples/Clinical/PatientIdentityMap.cls
  - [ ] Extend IdentityMap base class
  - [ ] Integrate with PatientActiveRecord or PatientRowGateway from previous stories
  - [ ] Implement GetOrLoadPatient(pId) method
  - [ ] Add cache invalidation on patient updates
  - [ ] Implement cache statistics for monitoring
  - [ ] Add scope management (session-level, request-level)
  - [ ] Implement cache warming for frequently accessed patients
  - [ ] Document healthcare-specific caching scenarios
  - [ ] Add example workflow showing duplicate load prevention

- [ ] Task 3: Create Comprehensive Unit Tests (AC: 3)
  - [ ] Create src/Patterns/Test/Unit/PoEAA/ObjectRelational/IdentityMapTest.cls
  - [ ] Test Add() stores object in map
  - [ ] Test Get() retrieves stored object
  - [ ] Test Get() returns same object instance (reference equality)
  - [ ] Test Contains() correctly identifies presence
  - [ ] Test Remove() removes object from map
  - [ ] Test Clear() empties entire map
  - [ ] Test GetOrLoad() loads object if not in map
  - [ ] Test GetOrLoad() returns cached object if in map
  - [ ] Test multiple objects with different IDs
  - [ ] Test cache statistics (hits, misses)
  - [ ] Test cache invalidation on updates
  - [ ] Test scope boundaries (session, request)
  - [ ] Test concurrent access if thread-safe
  - [ ] Test memory management (no leaks)
  - [ ] **CRITICAL**: All test methods return %Status and end with `Quit $$$OK`

- [ ] Task 4: Create Pattern Documentation (AC: 4)
  - [ ] Create docs/patterns/poeaa/identity-map.md
  - [ ] Include Intent, Motivation, and Structure sections
  - [ ] Add When to Use and When Not to Use sections
  - [ ] Document ObjectScript caching mechanisms
  - [ ] Explain cache invalidation strategies
  - [ ] Document scope management (session vs request vs transaction)
  - [ ] Include healthcare patient caching example walkthrough
  - [ ] Add comparison with simple caching and memoization
  - [ ] Document integration with Data Mapper and Unit of Work
  - [ ] Include Known Uses and Related Patterns sections

## Dev Notes

### Technical Context from Architecture

[Source: architecture/tech-stack.md]
- **Primary Language**: InterSystems ObjectScript (IRIS 2023.1+ compatible)
- **Testing Framework**: %UnitTest built-in framework  
- **No External Dependencies**: Pure ObjectScript implementation required
- **Namespace-agnostic**: Pattern must work in any namespace
- **Caching**: Use ObjectScript collections or multidimensional properties

### File Locations per Project Structure

[Source: architecture/source-tree.md]
- Pattern classes: src/Patterns/PoEAA/ObjectRelational/
  - IdentityMap.cls (base identity map class)
- Healthcare examples: src/Patterns/Examples/Clinical/
  - PatientIdentityMap.cls (patient caching example)
- Unit tests: src/Patterns/Test/Unit/PoEAA/ObjectRelational/
  - IdentityMapTest.cls
- Documentation: docs/patterns/poeaa/
  - identity-map.md

### Naming Conventions

[Source: architecture/coding-standards.md]
- Classes: PascalCase (IdentityMap, PatientIdentityMap)
- Methods: PascalCase for public operations (Add(), Get(), Contains(), Remove(), Clear())
- Properties: PascalCase (Cache, Statistics, HitCount, MissCount)
- Parameters: Prefix with 'p' (pId, pObject, pLoader)
- Local variables: Prefix with 't' (tObject, tExists, tSC)
- Constants: UPPERCASE with underscores as parameters

### Identity Map Pattern Specific Considerations

- **Single Instance Per ID**: Ensure same object instance returned for same ID
- **Reference Equality**: obj1 = obj2 when loaded with same ID
- **Cache Storage**: Use %ArrayOfObjects or multidimensional property
- **Lazy Loading**: GetOrLoad pattern loads only if not cached
- **Cache Invalidation**: Remove or update on object changes
- **Scope Management**: Session-level, request-level, or transaction-level
- **Statistics Tracking**: Monitor hits, misses, cache size
- **Memory Management**: Clear cache at appropriate boundaries
- **Thread Safety**: Consider concurrent access patterns
- **Integration**: Works with Data Mapper and Unit of Work patterns

### Identity Map Implementation Patterns

[Source: architecture/coding-standards.md]

**Add Pattern:**
```objectscript
Method Add(pId As %String, pObject As %RegisteredObject) As %Status
{
    Set tSC = $$$OK
    
    Try {
        // Store in map
        Do ..Cache.SetAt(pObject, pId)
        
        // Update statistics
        Set ..Size = ..Size + 1
        
    } Catch ex {
        Set tSC = ex.AsStatus()
    }
    
    Quit tSC
}
```

**Get Pattern:**
```objectscript
Method Get(pId As %String) As %RegisteredObject
{
    Set tObject = ""
    
    Try {
        // Check if exists
        If ..Cache.IsDefined(pId) {
            Set tObject = ..Cache.GetAt(pId)
            
            // Update hit count
            Set ..HitCount = ..HitCount + 1
        } Else {
            // Update miss count
            Set ..MissCount = ..MissCount + 1
        }
        
    } Catch ex {
        // Log error
    }
    
    Quit tObject
}
```

**GetOrLoad Pattern:**
```objectscript
Method GetOrLoad(pId As %String, pLoader As %String) As %RegisteredObject
{
    Set tObject = ""
    
    Try {
        // Check cache first
        Set tObject = ..Get(pId)
        
        // If not in cache, load it
        If '$IsObject(tObject) {
            // Call loader method
            Set tObject = $CLASSMETHOD(pLoader, "Load", pId)
            
            // Add to cache if loaded successfully
            If $IsObject(tObject) {
                Do ..Add(pId, tObject)
            }
        }
        
    } Catch ex {
        // Log error
    }
    
    Quit tObject
}
```

### Healthcare Patient Identity Map Requirements

- **Patient Object Caching**: Cache PatientActiveRecord or domain objects
- **Duplicate Prevention**: Ensure same patient instance across requests
- **Cache Invalidation**: Clear cache on patient updates
- **Scope Management**: Session-level caching for patient objects
- **Statistics**: Track cache hits/misses for optimization
- **Cache Warming**: Pre-load frequently accessed patients
- **Memory Bounds**: Implement cache size limits if needed

### Previous Story Context

[Source: Stories 6.1-6.5 Dev Agent Records]
- **Data Mapper**: Integration point for identity map
- **Unit of Work**: Identity map prevents duplicate tracking
- **Active Record/Row Gateway**: Can benefit from identity map caching
- **CRITICAL Lessons**:
  - Test methods MUST return %Status and end with `Quit $$$OK`
  - Cannot use argumented QUIT within Try/Catch blocks
  - Use ^ClineDebug global for debugging
  - Parameter names cannot contain underscores - use camelCase

### Error Handling Requirements

[Source: architecture/coding-standards.md]
- Return %Status from methods that can fail
- Use $$$OK and $$$ERROR macros
- Implement proper Try/Catch blocks
- **CRITICAL**: Cannot use argumented QUIT within Try/Catch blocks
- Provide meaningful error messages

### Performance Considerations

[Source: architecture/coding-standards.md]
- O(1) lookup time with %ArrayOfObjects
- Cache size limits to prevent memory issues
- Clear cache at transaction/session boundaries
- Profile cache hit ratio for optimization
- Consider weak references for memory management

### Security Considerations

- Don't cache sensitive patient data beyond session scope
- Clear cache on user logout
- Implement proper access control checks
- Audit cache access for compliance

## Testing

### Testing Standards

[Source: architecture/test-strategy.md]
- Unit tests must achieve >90% code coverage
- Test all cache operations (Add, Get, Remove, Clear)
- Test identity guarantee (same instance)
- Test cache statistics
- Test scope boundaries
- All test methods must start with "Test" prefix
- Use %UnitTest.TestCase as base class
- **CRITICAL**: All test methods must return %Status and end with `Quit $$$OK`

### Specific Test Scenarios for Identity Map Pattern

- Test Add() stores object in map
- Test Get() retrieves stored object
- Test Get() returns identical instance (reference equality)
- Test Get() with non-existent ID returns empty
- Test Contains() correctly identifies presence
- Test Remove() removes object from map
- Test Clear() empties entire map
- Test GetOrLoad() loads if not cached
- Test GetOrLoad() returns cached if exists
- Test statistics tracking (hits, misses)
- Test multiple objects with different IDs
- Test cache invalidation
- Test scope boundaries
- Test concurrent access patterns
- Test memory cleanup

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-09 | 1.0.0 | Initial story creation for Identity Map Pattern (Epic 7, Story 1) | Bob (Scrum Master) |
